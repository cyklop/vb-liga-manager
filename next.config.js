/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // swcMinify is default and the option is deprecated/removed in newer Next.js versions
}

module.exports = nextConfig
